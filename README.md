# Web Toolbox

实用纯静态小工具集合站。零依赖、零后端、零 API Key，双击 `file://` 即可使用。

## 工具列表

| 工具 | 状态 | 说明 |
|------|------|------|
| 📖 [梦幻词栈 · 英语生词拾取器](./tools/en-words/) | ✅ 可用 | 粘贴英文 → 智能识别 TOP 生词 → 生词本复习 |
| ⏱ 番茄时钟 | 🔜 占位 | 简洁番茄工作法计时器 |
| { } JSON 格式化 | 🔜 占位 | JSON 格式化、校验、树形浏览 |
| 🎨 调色板工具 | 🔜 占位 | 颜色拾取、渐变生成 |

## 本地使用

```bash
# 直接双击打开（推荐）
open index.html

# 或启动简单 HTTP 服务
python -m http.server 8080
# 然后访问 http://localhost:8080
```

## 拾词 · 功能说明

「梦幻词栈」是一个离线英语生词识别工具：

-   **智能识别**：粘贴英文文本，自动分析词频，筛选 TOP 生词
-   **离线词典**：基于 CET4/CET6 分级词库，完全不依赖网络
-   **生词本**：加入生词本后可在复习页进行自测、标记掌握、导出
-   **纯浏览器**：所有数据存储在 localStorage，不离开你的设备

### 数据来源

离线词典数据来自 [ECDICT](https://github.com/skywind3000/ECDICT)（skywind3000 开源项目），
选取 CET4/CET6 词条构建子集，在此致谢。

## 技术路线

-   纯静态 HTML/CSS/JS，零外部依赖
-   词典数据以 `window.EWDICT` 形式通过 `<script>` 加载
-   识别引擎使用策略模式，为二期 AI 识别预留扩展点
-   全站支持 `file://` 协议直接打开

## 后续计划

_暂不具体描述，占位。_
